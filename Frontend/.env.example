VITE_SUPABASE_URL=https://YOUR_SUPABASE_PROJECT_URL.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=
# Leave VITE_API_URL unset to use the same-origin /api proxy
# (Netlify redirect in production, Vite proxy in dev). Override only
# when pointing the app at a different backend URL directly.
VITE_API_URL=
